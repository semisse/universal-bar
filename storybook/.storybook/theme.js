import { create } from '@storybook/theming';

export default create({
  base: 'light',
  brandTitle: 'Nuxeo Elements Catalog',
  brandUrl: 'https://nuxeo.com',
  brandImage: '/nuxeo-elements-catalog.svg',
});
